# Backend dependencies. Install into the shared venv:
#   python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt
-r requirements-core.txt

# On-device models loaded in-process (the Docker backend calls the model containers instead)
transformers==5.17.0
accelerate==1.15.0

# Steps 0-1: recorder + on-device transcription (LFM2.5-Audio-1.5B, needs Python >= 3.12)
liquid-audio==1.3.0
torch==2.14.0
torchaudio==2.11.0
sounddevice==0.5.6

# Tests
pytest==9.1.1
