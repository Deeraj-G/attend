# Backend runtime when the models run in their own containers (docker-compose.yml).
# requirements.txt includes this and adds the in-process model stack for host dev.
fastapi==0.141.1
uvicorn[standard]==0.54.0
pydantic==2.13.5
pydantic-settings==2.15.0
python-dotenv==1.2.3
httpx==0.28.1
python-multipart==0.0.32
websockets==17.1

# Step 0: recorder, and chunking audio before transcription
numpy==2.5.3
soundfile==0.14.0
